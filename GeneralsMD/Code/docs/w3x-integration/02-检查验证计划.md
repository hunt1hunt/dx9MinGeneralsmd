# W3X 集成 — 检查验证计划

## 总则

每阶段均有明确的可验证检查点，分三级：
- **编译级**：编译通过即验证
- **日志级**：运行时有诊断输出
- **视觉级**：实际渲染效果验证

---

## Phase 1 验证：基础数据结构

### 编译级

```
- w3d_file.h 新增 6 个常量 → 编译通过
- meshgeometry.h 新增方法 → 编译通过
- meshmdlio.cpp 新增 case → 编译通过
- pugixml 引入 → 编译通过
```

### 日志级

```
[W3X_P1] VERTEX_CHANNEL_TANGENT=0x20 VERTEX_CHANNEL_BINORMAL=0x40
[W3X_P1] CHUNK_TANGENTS=0x60 CHUNK_BINORMALS=0x61
[W3X_P1] CHUNK_FX_SHADERS=0x50-0x53 added
[W3X_P1] mesh 'XXX' has TANGENTS chunk: N vertices loaded from file
[W3X_P1] mesh 'XXX' NO tangents in file, will compute at runtime
```

### 诊断触发器

```ini
[Debug]
W3XDiagnostics = Yes        ; 启用 W3X 模块诊断输出
```

---

## Phase 2 验证：W3X 加载器

### 编译级

```
- w3x_loader.h/cpp 新增 → 编译通过
- pugixml 正确链接 → 编译通过
```

### 日志级

加载一个子网格 W3X 文件后的完整诊断输出：

```
[W3X_P2] W3XLoader::LoadMesh('APATAVGATTTANK_SKN.SKIN_BODY01.w3x')
[W3X_P2]   File size: 284KB
[W3X_P2]   XML parse: OK (2.3ms)
[W3X_P2]   Mesh ID: 'APATAVGATTTANK_SKN.SKIN_BODY01'
[W3X_P2]   GeometryType: Skin
[W3X_P2]   CastShadow: true
[W3X_P2] ========== Vertex Data ==========
[W3X_P2]   Vertices: 3510 ✓ (expected range: 3000-4000)
[W3X_P2]   Normals: 3510 ✓
[W3X_P2]   Tangents: 7020 ← 2x vertices (UV seams split)
[W3X_P2]   Binormals: 3510 ✓
[W3X_P2]   UV channels: 1 (3510 coords)
[W3X_P2]   VertexColors: no
[W3X_P2]   VertexInfluences: 3510 (bones: 16)
[W3X_P2] ========== Face Data ==========
[W3X_P2]   Triangles: 7020
[W3X_P2]   SurfaceType: 0 (default)
[W3X_P2] ========== Material ==========
[W3X_P2]   FXShader: objectssoviet.fx
[W3X_P2]   Technique: 0
[W3X_P2]   Constants: 5 entries
[W3X_P2]     [0] Texture DiffuseTexture = 'TavGattTank2'
[W3X_P2]     [1] Texture NormalMap = 'TavGattTank2_NRM' ← 法线贴图引用
[W3X_P2]     [2] Texture SpecMap = 'TavGattTank2_SPM'
[W3X_P2]     [3] Float EnvMult = 1.000000
[W3X_P2]     [4] Bool AlphaTestEnable = true
[W3X_P2] ========== Bounding ==========
[W3X_P2]   AABB: min=(-22.8,-11.8,2.6) max=(24.0,11.9,22.6)
[W3X_P2]   Sphere: center=(0.5,5.8,6.8) radius=29.0
[W3X_P2] ========== Result ==========
[W3X_P2]   Load result: ✓ PASS
[W3X_P2]   MeshModelClass: allocated 3510 verts, 7020 faces
```

加载容器文件（W3DContainer）的诊断输出：

```
[W3X_P2] W3XLoader::LoadContainer('APATAVGATTTANK_SKN.w3x')
[W3X_P2]   Container ID: 'APATAVGATTTANK_SKN'
[W3X_P2]   Hierarchy: 'APATAVGATTTANK_SKL'
[W3X_P2]   SubObjects: 9
[W3X_P2]     SKIN_SP01  → bone=0  Mesh:APATAVGATTTANK_SKN.SKIN_SP01
[W3X_P2]     BOX04      → bone=0  CollisionBox
[W3X_P2]     SKIN_UP01  → bone=0  Mesh:APATAVGATTTANK_SKN.SKIN_UP01
[W3X_P2]     SKIN_UP02  → bone=0  Mesh:APATAVGATTTANK_SKN.SKIN_UP02
[W3X_P2]     SKIN_UP03  → bone=0  Mesh:APATAVGATTTANK_SKN.SKIN_UP03
[W3X_P2]     BOX03      → bone=0  CollisionBox
[W3X_P2]     SKIN_BODY01 → bone=0 Mesh:APATAVGATTTANK_SKN.SKIN_BODY01  ← 车身
[W3X_P2]     SKIN_WHEEL → bone=0  Mesh:APATAVGATTTANK_SKN.SKIN_WHEEL  ← 轮子
[W3X_P2]     SKIN_TREAD → bone=0  Mesh:APATAVGATTTANK_SKN.SKIN_TREAD  ← 履带
```

纹理解析诊断：

```
[W3X_P2] ResolveTexture('TavGattTank2_NRM')
[W3X_P2]   TextureXML: E:\红警3解压后资源文件\TavGattTank2_NRM.xml
[W3X_P2]   Parse: OK
[W3X_P2]   DDS File: TavGattTank2_NRM.dds
[W3X_P2]   Format: DXT5
[W3X_P2]   Result: DDS found ✓ (2048×2048)
```

### 边界情况验证

| 测试场景 | 预期 |
|---------|------|
| 不存在的 w3x 文件 | 返回 false，日志警告 |
| XML 格式错误 | 返回 false，日志输出错误行号 |
| 缺少 Vertices 节点 | 返回 false，日志指出缺少节点 |
| UV 坐标缺失 | 构建默认 UV（全 0） |
| 大量顶点（>10万） | 正常加载，日志报告大模型警告 |
| 无 Tangent 数据 | Tangent 数组为 NULL，不做处理 |
| 无 FXShader 引用 | Constants 列表为空，不报错 |

---

## Phase 3 验证：Effect 管理器

### 编译级

```
- W3XEffectManager 编译通过
```

### 日志级

```
[W3X_P3] EffectManager::GetEffect('Shaders/objectssoviet.fx')
[W3X_P3]   Reading file... 1546 bytes
[W3X_P3]   Parsing macros: 6 defines active
[W3X_P3]   D3DXCreateEffect... OK (12.5ms)
[W3X_P3]   Effect desc:
[W3X_P3]     Techniques: 3
[W3X_P3]     Parameters: 28
[W3X_P3]       float4x4 WorldViewProj  ← ⚡ auto-bound ✓
[W3X_P3]       float4x4 World          ← ⚡ auto-bound ✓
[W3X_P3]       float3 SunDirection     ← ⚡ auto-bound ✓
[W3X_P3]       float3 SunColor         ← ⚡ auto-bound ✓
[W3X_P3]       float3 AmbientLightColor ← ⚡ auto-bound ✓
[W3X_P3]       float3 EyePosition      ← ⚡ auto-bound ✓
[W3X_P3]       float3 RecolorColor      ← ⚡ auto-bound ✓
[W3X_P3]       float OpacityOverride    ← ⚡ auto-bound ✓
[W3X_P3]       ... 20 parameters (shader internal)
[W3X_P3]   Auto-bound: 8/28 ✓
[W3X_P3]   Effect cache: 1 entries (Shaders/objectssoviet.fx)
[W3X_P3]
[W3X_P3] EffectManager::GetEffect('Shaders/objectsalliedtread.fx')
[W3X_P3]   D3DXCreateEffect... OK
[W3X_P3]   Parameters: 27 (SUPPORT_TREAD_SCROLLING adds 2 extra)
[W3X_P3]   Effect cache: 2 entries
[W3X_P3]
[W3X_P3] --- Device Lost/Reset Test ---
[W3X_P3] OnLostDevice: 2 effects notified ✓
[W3X_P3] OnResetDevice: 2 effects recreated ✓
[W3X_P3]   Effect 1: Shaders/objectssoviet.fx → D3D_OK (no compile needed) ✓
[W3X_P3]   Effect 2: Shaders/objectsalliedtread.fx → D3D_OK ✓
```

### 性能验证

```ini
[Debug]
W3XDumpPerf = Yes
```

```
[W3X_PERF] Effect creation: Shaders/objectssoviet.fx = 12.5ms (one-time)
[W3X_PERF] Effect creation: Shaders/objectsalliedtread.fx = 11.2ms (one-time)
[W3X_PERF] BindEngineConstants: 0.03ms/frame
[W3X_PERF] Effect cache hit ratio: 100% (12 instances × 2 effects)
```

### 兼容性验证

| .fx 特性 | 状态 |
|---------|------|
| fx_2_0  profile | ✅ |
| vs_2_0 / ps_2_0 指令 | ✅ |
| texture2D / textureCUBE | ✅ |
| technique / pass / compile | ✅ |
| #include .FXH 链 (head0→head1→head2→head3) | ✅ |
| 宏条件编译 (`#ifdef DYNAMIC_CLOUD_REF`) | ✅ |
| 寄存器绑定 (`: register(ps, c0)`) | ✅ |
| 纹理采样器 (`sampler DiffuseTextureSampler`) | ✅ |

---

## Phase 4 验证：W3XModelDraw

### 编译级

```
- W3XModelDraw.h/cpp 编译通过
- INI 解析器识别 W3XModelDraw → 编译通过
```

### 日志级

**INI 解析验证：**
```
[W3X_P4] INI: parsing W3XModelDraw block...
[W3X_P4]   Model = APATAVGATTTANK_SKN.w3x ✓
[W3X_P4]   FXShader = Shaders/objectssoviet.fx ✓
[W3X_P4]   Technique = 0 ✓
[W3X_P4]   OverrideConstant: DiffuseTexture = TEXTURE:TavGattTank2_D ✓
[W3X_P4]   OverrideConstant: NormalMap = TEXTURE:TavGattTank2_NRM ✓
[W3X_P4]   OverrideConstant: SpecMap = TEXTURE:TavGattTank2_SPM ✓
[W3X_P4]   OverrideConstant: EnvMult = FLOAT:1.0 ✓
[W3X_P4]   OverrideConstant: AlphaTestEnable = BOOL:true ✓
[W3X_P4]   BoneRemap: Bone_Turret01 → TurretBarrel ✓
[W3X_P4]   BoneRemap: Bone_Muzzle → FirePoint ✓
[W3X_P4]   ConditionState: NONE ✓
[W3X_P4]   ConditionState: DAMAGED ✓
[W3X_P4]   Translucent: No ✓
[W3X_P4]   Parse result: 14 fields OK, 0 errors →
```

**初始化验证：**
```
[W3X_P4] W3XModelDraw::init() 'APATAVGATTTANK'
[W3X_P4]   Step 1/4: Load container... OK (9 sub-objects)
[W3X_P4]   Step 2/4: Get/Create effects... 
[W3X_P4]     'Shaders/objectssoviet.fx' → cache hit ✓ (refcount: 5)
[W3X_P4]     'Shaders/objectsalliedtread.fx' → cache hit ✓ (refcount: 2)
[W3X_P4]   Step 3/4: Build vertex buffers...
[W3X_P4]     Sub-mesh 'SKIN_BODY01': VB 3510 verts, IB 21060 indices ✓
[W3X_P4]     Sub-mesh 'SKIN_TREAD': VB 384 verts, IB 2304 indices ✓
[W3X_P4]     ...7 sub-meshes total, 45.2KB vertex data
[W3X_P4]   Step 4/4: Bind instance constants...
[W3X_P4]     Per-instance constants applied: 5/5 ✓
[W3X_P4]   Init result: ✓ PASS (elapsed: 34.7ms)
```

### 渲染验证（截帧）

**每帧诊断（`W3XDumpFrame = Yes`）：**
```
[W3X_RENDER] Frame 1234
[W3X_RENDER]   Instance: APATAVGATTTANK (ID=0x7F459A)
[W3X_RENDER]   LOD selected: 0 (dist=45.2m, range=0-100m)
[W3X_RENDER]   Sub-mesh 'SKIN_BODY01': 
[W3X_RENDER]     Effect: objectssoviet.fx, Technique 0
[W3X_RENDER]     Pass 0/4: D3DRS_ALPHABLENDENABLE=FALSE ✓
[W3X_RENDER]     Pass 1/4: DrawIndexedPrimitive(3510 verts, 7020 tris) ✓
[W3X_RENDER]     Pass 2/4: Shadow pass ✓
[W3X_RENDER]     Pass 3/4: Depth only ✓
[W3X_RENDER]   Sub-mesh 'SKIN_TREAD':
[W3X_RENDER]     Effect: objectsalliedtread.fx, Technique 0
[W3X_RENDER]     Pass 0/3: DrawIndexedPrimitive(384 verts, 768 tris) ✓
[W3X_RENDER]     Pass 1/3: Tread scrolling UV offset update ✓
[W3X_RENDER]     Pass 2/3: Depth only ✓
[W3X_RENDER]   Total W3X instances: 12
[W3X_RENDER]   Total W3X draw calls: 48
[W3X_RENDER]   Total W3X triangles: 56832
[W3X_RENDER]   Effect cache: 2 effects, 12 instances sharing
[W3X_RENDER]   Parameter bind errors: 0
```

### 视觉验证

| 测试 | 方法 | 预期 |
|------|------|------|
| W3X 模型在场景中显示 | 在自定义 map 中放置单位 | 模型正确显示，法线贴图可见 |
| 法线贴图生效 | 对比有无法线贴图的渲染 | 有法线贴图时表面细节明显 |
| 履带滚动 | 观察履带纹理 | UV 随时间滚动 |
| 阵营色 | 不同队伍颜色 | 车体显示对应阵营色 |
| 损坏状态 | 攻击单位到 DAMAGED | 模型切换为 _DMG 版本 |
| LOD 切换 | 拉远镜头 | 模型切换到低 LOD（三角形减少） |
| 与 W3D 模型共存 | 同一场景中放置 W3D 和 W3X 单位 | 两者都能正确显示 |
| Alpha 镂空 | 观察有透明纹理的部件 | 镂空正确 |
| 透明度排序 | 观察透明 W3X 物体 | 正确排序（不遮挡） |

### 性能验证

```ini
[Debug]
W3XDumpPerf = Yes
```

```
[W3X_PERF] Frame 1234
[W3X_PERF]   W3X instances: 12
[W3X_PERF]   VB/IB memory: 542KB (total)
[W3X_PERF]   Effect prep: 0.12ms
[W3X_PERF]   Render: 1.34ms (48 draw calls)
[W3X_PERF]   Effect switches: 2 (2 unique .fx files)
[W3X_PERF]   State changes: 12 texture binds, 28 parameter sets
[W3X_PERF]   Total W3X GPU time: 1.46ms
```

### 兼容性验证

| 场景 | 预期 |
|------|------|
| D3D9 Reference Rasterizer | 正常运行（慢） |
| D3D9On12 | 正常运行 |
| 窗口切换 Alt+Tab | OnLostDevice/OnResetDevice 正确执行 |
| 多卡 SLI/CF | 正常运行 |
| 无 DDS 纹理 | 加载默认白色纹理，不崩溃 |

---

## Phase 5 验证：W3D 切线 + _n.dds

### 日志级

```
[W3X_P5] Mesh 'AMGeneral' tangent status
[W3X_P5]   Tangents from file: no (no 0x60/0x61 chunk)
[W3X_P5]   Computing at runtime:
[W3X_P5]     Method: MikkTSpace (from INI setting)
[W3X_P5]     Lengyel: minAngle=0.01°, maxAngle=178°
[W3X_P5]     MikkTSpace: 3510 verts → 7020 processed (UV seams split)
[W3X_P5]   Validation:
[W3X_P5]     Orthogonality: avg(T·N)=0.00 ✓
[W3X_P5]     Orthogonality: avg(B·N)=0.00 ✓
[W3X_P5]     Orthogonality: avg(T·B)=0.02 (near orthogonal) ✓
[W3X_P5]     Right-handed: 99.7% ✓ (N == cross(T,B))
[W3X_P5]   Result: tangents computed ✓ (elapsed: 2.3ms)

[W3X_P5] _n.dds probe for 'AMGeneral':
[W3X_P5]   Probing: ART/Textures/AMGeneral_n.dds
[W3X_P5]   File exists: YES
[W3X_P5]   Loading: AMGeneral_n.dds → TextureClass allocated
[W3X_P5]   Format: DXT5, 2048×2048, 10 mips
[W3X_P5]   Registered: PBR_STAGE_NORMAL (stage 1) ✓
```

### 视觉验证

| 测试 | 方法 | 预期 |
|------|------|------|
| 法线贴图效果 | W3D 单位加 _n.dds | 表面凹凸细节明显 |
| 无 _n.dds 回退 | 去掉 _n.dds 文件 | 模型正常显示，无非表面细节，不崩溃 |
| 切线计算正确性 | 比较软件渲染对照 | 光照方向和强度匹配 |
| UV 接缝 | 观察 UV 接缝处 | MikkTSpace 模式下接缝不明显 |

---

## Phase 6 验证：提取工具

### 输出验证

```
命令行：python ra3_import.py --model APATAVGATTTANK --src D:/红警3解压后资源文件 --output D:/MyMod/

=== RA3 Import Report ===
Model: APATAVGATTTANK
Source: D:/红警3解压后资源文件

[1/8] Scanning for model files... found 13 .w3x files ✓
[1/8]   Primary container: APATAVGATTTANK_SKN.w3x
[2/8] Parsing W3X sub-meshes... 9 sub-objects ✓
[3/8] FXShader analysis:
[3/8]   Shaders needed: 2
[3/8]     objectssoviet.fx → [FOUND] → 5 sub-meshes use this
[3/8]     objectsalliedtread.fx → [FOUND] → 2 sub-meshes use this
[4/8] Texture extraction:
[4/8]   TavGattTank2_D → TavGattTank2_D.dds ✓
[4/8]   TavGattTank2_NRM → TavGattTank2_NRM.dds ✓ (法线贴图)
[4/8]   TavGattTank2_SPM → TavGattTank2_SPM.dds ✓
[4/8]   TavGattTank2_K → TavGattTank2_K.dds ✓
[4/8]   TavGattTank2 → TavGattTank2.dds ✓
[5/8] Audio extraction: 6 WAV files
[6/8] Copying files... 22 files copied
[7/8] Generating INI bridge... W3X_APATAVGATTTANK.ini
[8/8] Writing report... done

Summary:
  Models: 13/13 copied ✓
  Textures: 5/5 ✓
  Shaders: 2/2 ✓
  Audio: 6/6 ✓
  INI bridge: generated ✓
  Total size: 8.4MB
  Time: 2.3s

✅ Import complete! Copy 'INI/W3X_APATAVGATTTANK.ini' into your mod.
```

---

## 集成测试（全流程）

在以上各阶段验证通过后，运行以下全流程测试：

### 测试 1：纯 W3D 回归

```
1. 加载原版 Generals 关卡
2. 所有 W3D 模型正常显示
3. 无新警告/错误
4. FPS 不下降 >5%
```

### 测试 2：W3D + _n.dds 法线

```
1. 给一个原有 W3D 模型添加 _n.dds
2. 运行时触发切线计算
3. 法线贴图正确显示
```

### 测试 3：纯 W3X 模型加载

```
1. 在自定义地图放置 W3X 单位
2. model = APATAVGATTTANK_SKN.w3x
3. effect = Shaders/objectssoviet.fx
4. 模型正确显示，法线贴图生效
5. 履带纹理滚动动画正常
```

### 测试 4：W3D + W3X 混合场景

```
1. 同一战场中既有 W3D 单位又有 W3X 单位
2. 两者都能正确渲染
3. 阴影投射正确
4. 点击选中/框选正常
```
